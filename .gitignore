# Local Python environment and generated bytecode are never versioned.
.venv/
__pycache__/
*.py[cod]

# Visual Studio per-user workspace state is never versioned.
.vs/

# Runtime application and operator-session logs remain local. Keep source,
# configuration, documentation, and deliberate test fixtures under logs/ tracked.
logs/*.log
